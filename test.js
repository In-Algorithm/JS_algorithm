const amounts = [1000.0, 2000.0, 3000.0];
let input1 = 1;
let input2 = 2;
let input3 = 3;
for (let amount of amounts) {
  const input = [input1, input2, input3];
  console.log(input[0]);
  // let outstanding = 0;
  // outstanding += input[count - 1] + amount * count;
  // this.printOutstanding(count, outstanding);
  // count++;
  // sumOutstanding += outstanding;
}
